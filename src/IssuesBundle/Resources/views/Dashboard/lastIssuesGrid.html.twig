{% extends 'OroDashboardBundle:Dashboard:widget.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}
    {{ dataGrid.renderGrid('dashboard-recent-issues-grid') }}
{% endblock %}

{% block actions %}
    {% set actions = [{
    'url': path(
        'issues.issues_collaborated_recently',
        {
            "grid[dasbhoard-recente-issues-grid]": 1,
            "p": 100,
            "c": "code1.summary1.tags1",
            "v": "__all__"
        }
    ),
    'type': 'link',
    'label': 'issues.collaborated_recently.view_all'|trans
    }] %}

    {{ parent() }}
{% endblock %}